apos.define('apostrophe-i18n-debugger-modal', {
  extend: 'apostrophe-modal',
  source: 'modal',
  construct: function(self, options) {
    // Everything is global handlers in user.js, to be
    // more futureproof if we create a debug toolbar
  }
});
